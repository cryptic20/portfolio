version: '3.7'

services:
  frontend:
    container_name: frontend
    build:
      context: ./front-end
      dockerfile: Dockerfile
    volumes:
      - './front-end:/app'
      - '/app/node_modules'
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
